from distutils.core import setup

setup(name='PyFastFit',
      version='${FastFit_VERSION_MAJOR}.${FastFit_VERSION_MINOR}',
      package_data={'PyFastFit': ['*.so']},
      packages=['PyFastFit'])

